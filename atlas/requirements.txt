# Atlas daemon dependencies (separate from Klipper's own requirements —
# Atlas is a standalone companion, and its deterministic floor must run
# with only the standard library).  PyYAML is needed only to load the
# on-disk YAML failure-pattern catalog; the schema/matcher core works on
# plain dicts with no third-party dependency.
PyYAML>=6.0
