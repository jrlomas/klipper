# Atlas daemon dependencies (separate from Klipper's own requirements).
# PyYAML loads the on-disk failure-pattern catalog; PyNaCl supplies the
# fail-closed Ed25519 verifier used by signed firmware and KB activation.
PyYAML>=6.0
PyNaCl>=1.5.0
